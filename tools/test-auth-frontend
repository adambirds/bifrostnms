#!/usr/bin/env bash
set -Eeuo pipefail

cd "$(git rev-parse --show-toplevel)"

echo "🧪 Checking BifrostNMS auth frontend..."
pnpm --dir auth-frontend typecheck
pnpm --dir auth-frontend lint
pnpm --dir auth-frontend build
