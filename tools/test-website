#!/usr/bin/env bash
set -Eeuo pipefail

cd "$(git rev-parse --show-toplevel)"

echo "🧪 Checking BifrostNMS dashboard..."
pnpm --dir frontend typecheck
pnpm --dir frontend lint
pnpm --dir frontend build
