# Agent enrolment UI

The dashboard issues short-lived, single-use enrolment tokens for existing agent records through the realm-scoped management API. Raw enrolment tokens are displayed only from the creation response and are not recoverable from ordinary read APIs.
